// this file contains some tricky mistakes


const /*adasfging ercons */a = (-1++++1+-1+-(-2)*-(-1); //this is legal because '+' is recursive, however, it's illegal while using clang
const b[]={1,2,3,4,5,6,7,8,9+9,2*(3 2};
void main()
{
    	int i, j, temp, a, b;
	
	b(;
	
	b = (((1+2) *2 *2+a/b+3); 
	a = (1 1 + 1))) /*ssdfhsdjf */+ (1)); // recovery for this exp is tricky
	a = a b + c +1+-1+-(-2)*-(-1);
	a = 2+3*2/(3 +2);
}

void b(
{
	while (1  2 + 2 != 2  3
	{
		;	
	}
}

void c)
{
	if (a == b+ 1)
		if (b == c++c);
	else 
		while ( b > 1;
}
